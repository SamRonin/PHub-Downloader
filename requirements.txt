# NOTE: yt-dlp and curl_cffi are pinned together on purpose.
# PornHub blocks non-browser clients, so yt-dlp must fetch pages through
# curl_cffi's browser impersonation. Unpinned versions made every Railway
# deploy install a different yt-dlp/curl_cffi pair, which is why the same
# code worked in one deployment and failed with
# "Redirection detected / HTTP 403" in the next.
# To upgrade: bump BOTH lines together, test once, then deploy.
aiogram>=3.13,<4
aiosqlite>=0.20
httpx>=0.27
# PornHub thumbnail CDN serves JPEG/WebP/AVIF unpredictably; pillow normalises
# whatever it returns to a JPEG that Telegram always accepts as a photo.
pillow>=10.0
yt-dlp[default,curl-cffi]==2026.8.19
curl_cffi==0.16.3

brotli
certifi
cffi
charset-normalizer
idna
markdown-it-py
mdurl
mutagen
pip
pycparser
pycryptodome
Pygments
requests
rich
urllib3
websockets
yt-dlp
yt-dlp-ejs
requests-toolbelt
